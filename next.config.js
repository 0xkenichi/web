/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // NEXT_PUBLIC_API_BASE_URL is automatically available to client-side code
  // Set it in .env.local file in this directory
};

module.exports = nextConfig;

